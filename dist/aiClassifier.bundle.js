/**
 * Stub for AgileEmails AI classifier. Replace with real bundle after: npm install && npm run build
 * When this stub is used, classify() always returns null and the extension falls back to rule-based classification.
 */
(function() {
  'use strict';
  window.agileEmailsAIClassifier = {
    init: function() { return Promise.resolve(); },
    classify: function() { return Promise.resolve(null); }
  };
})();
